<?php

 /*
    |--------------------------------------------------------------------------
    | 定义根据请求地址生成动态样式名称
    |--------------------------------------------------------------------------
    |
    */

function route_class()
{
    return str_replace('.', '-', Route::currentRouteName());
}